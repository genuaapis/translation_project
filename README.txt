Build using the "make" command. The makefile is entitled, "makefile."
Invoking "make clean" will remove all object files, the executable, and all output files.

Invocations:
./P0
./P0 < [file_name]
./P0 [file_name_trunc], where "file_name_trunc" refers to a file name given without the extension .sp2022.
